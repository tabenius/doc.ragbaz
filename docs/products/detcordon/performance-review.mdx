---
title: "DetCordon — Performance & Overhead Review"
sidebar_position: 11.3
description: "Measured request overhead for DetCordon and gatekeeper in front of the same WordPress origin, with resource-consumption breakdowns for stakeholders, customers, and technical buyers."
---

import { DonutChart, GroupedBarChart, StatGrid, ChartPanel, PALETTE } from '@site/src/components/PerfReviewCharts';

# DetCordon — Performance & Overhead Review

{/*
  BENCHMARK_DATA — single source of truth for every chart on this page.
  Methodology and raw numbers are in the "How this was measured" section
  below. Update this block, not the charts, when re-running the benchmark.
*/}
export const BENCH = {
  // Single-connection run (ab -n 200 -c 1) -- isolates per-request proxy
  // overhead from the origin's own throughput ceiling.
  requestsPerSecSingleConn: { wordpress: 15.87, gatekeeper: 15.69, detcordon: 6.72 },
  latencyMs: {
    categories: ['p50', 'p95', 'p99'],
    wordpress: [60, 75, 152],
    gatekeeper: [62, 74, 84],
    detcordon: [89, 361, 820],
  },
  transferKbSecSingleConn: { wordpress: 1063.39, gatekeeper: 1054.18, detcordon: 450.65 },
  // Concurrent run (ab -n 1000 -c 20, 800 for the CPU-sampling run) -- the
  // single-threaded PHP dev-server origin used for this test is itself the
  // bottleneck under concurrency, which compresses the req/sec gap between
  // configs; see "How this was measured" for what that does and doesn't tell you.
  requestsPerSecConcurrent: { wordpress: 14.43, gatekeeper: 13.24, detcordon: 12.38 },
  transferKbSecConcurrent: { wordpress: 966.72, gatekeeper: 889.68, detcordon: 829.28 },
  cpuPctUnderLoad: { gatekeeper: 1.0, detcordon: 5.0 },
  memoryMb: { gatekeeper: 9.8, detcordon: 33.6 },
  diskMb: { gatekeeper: 9.3, detcordon: 8.0 },
  // Derived, not separately profiled: proxy/network overhead = gatekeeper's
  // measured p50 delta over plain WordPress (gatekeeper does real proxying
  // with no rule engine, so it isolates that cost); ModSecurity+CRS = what's
  // left of DetCordon's total p50 delta after subtracting that same proxy cost.
  detcordonTimeBreakdownMs: { wordpress: 60, proxyOverhead: 2, modsecurity: 27 },
};

This is a real, measured comparison — not a marketing estimate. We ran three
identical WordPress origins behind three different front doors and timed
every request. If you're deciding whether you need a hardened ingress
([gatekeeper](/products/articulate/gatekeeper)), a full malware-observation
layer ([DetCordon](/products/detcordon)), or neither, the numbers below are
the actual cost of each choice.

**In one sentence:** gatekeeper costs you almost nothing and stops a
narrow, real class of attacks; DetCordon costs you real request latency and
buys you full visibility into anything that gets through — they answer
different questions, and the right answer for most teams is "both, in
different places."

## The three configurations

| | What it is | What it does to a request |
| --- | --- | --- |
| **Plain WordPress** | A stock WordPress install with nothing in front of it | Nothing — this is the baseline every other number is measured against |
| **[Gatekeeper](/products/articulate/gatekeeper)** | A thin Rust ingress: hardened upload endpoint, everything else passed straight through | Buffers the body, forwards it, and inspects file uploads specifically — no line-by-line inspection of ordinary traffic |
| **[DetCordon](/products/detcordon)** | A full observation tap: ModSecurity + the OWASP Core Rule Set, in both directions, plus eBPF telemetry | Runs the *entire* request and the *entire response* through the CRS rule engine before either one is allowed through |

Neither product is trying to be the other. Gatekeeper's job is to keep a
production WordPress site from getting popped in the first place, cheaply
enough to run on every request all the time. DetCordon's job is the
opposite: assume something *will* get through somewhere, and give you a
safe, contained place to watch it happen and capture what it drops. That
difference in purpose is exactly why the performance profiles below look so
different — DetCordon is not a worse gatekeeper, it is doing categorically
more work per request on purpose.

## Performance metrics

### Headline numbers

<StatGrid stats={[
  { label: 'p50 latency', value: `${BENCH.latencyMs.detcordon[0]} ms`, sublabel: `DetCordon vs ${BENCH.latencyMs.wordpress[0]} ms baseline`, color: PALETTE.detcordon },
  { label: 'p99 latency', value: `${BENCH.latencyMs.detcordon[2]} ms`, sublabel: `DetCordon vs ${BENCH.latencyMs.wordpress[2]} ms baseline`, color: PALETTE.detcordon },
  { label: 'CPU under load', value: `${BENCH.cpuPctUnderLoad.detcordon}%`, sublabel: `vs gatekeeper's ${BENCH.cpuPctUnderLoad.gatekeeper}%`, color: PALETTE.detcordon },
  { label: 'Memory (RSS)', value: `${BENCH.memoryMb.detcordon} MB`, sublabel: `vs gatekeeper's ${BENCH.memoryMb.gatekeeper} MB`, color: PALETTE.detcordon },
]} />

### Latency, by percentile

<ChartPanel
  title="Request latency (p50 / p95 / p99)"
  subtitle="Lower is better. Single-connection runs, so this isolates per-request proxy overhead from WordPress's own throughput ceiling."
>
  <GroupedBarChart
    categories={BENCH.latencyMs.categories}
    unit=" ms"
    series={[
      { name: 'Plain WordPress', color: PALETTE.wordpress, values: BENCH.latencyMs.wordpress },
      { name: 'Gatekeeper', color: PALETTE.gatekeeper, values: BENCH.latencyMs.gatekeeper },
      { name: 'DetCordon', color: PALETTE.detcordon, values: BENCH.latencyMs.detcordon },
    ]}
  />
</ChartPanel>

Gatekeeper adds almost nothing at p50/p95 and stays flat at the tail — it's
a thin pass-through and it behaves like one. DetCordon adds real time
everywhere, and the gap *widens sharply* at the tail: p50 is +29ms over
baseline, but p99 is +668ms. That's the OWASP Core Rule Set doing real
regex/pattern work, and — on the single-core host this was measured on —
that work occasionally queues behind other in-flight requests, which is
exactly the kind of tail-latency risk a production deployment needs to plan
capacity around, not something to paper over.

### Throughput under concurrent load

<ChartPanel
  title="Requests per second (20 concurrent connections)"
  subtitle="Higher is better -- but read the caveat below before drawing conclusions from this one."
>
  <GroupedBarChart
    categories={['requests/sec']}
    series={[
      { name: 'Plain WordPress', color: PALETTE.wordpress, values: [BENCH.requestsPerSecConcurrent.wordpress] },
      { name: 'Gatekeeper', color: PALETTE.gatekeeper, values: [BENCH.requestsPerSecConcurrent.gatekeeper] },
      { name: 'DetCordon', color: PALETTE.detcordon, values: [BENCH.requestsPerSecConcurrent.detcordon] },
    ]}
  />
</ChartPanel>

**Read this chart carefully.** Under 20 concurrent connections, the three
configurations look surprisingly close (14.4 vs 13.2 vs 12.4 req/s) —
*much* closer than the latency chart above would suggest. That's because
this benchmark's WordPress origin (PHP's built-in single-threaded dev
server, chosen so all three configs share one identical, honestly-labeled
origin) is itself the bottleneck at this concurrency level: it can't
process requests fast enough for the front door's overhead to be the
limiting factor. This is a real, useful data point — it shows that when
your origin is the bottleneck, the *relative* cost of your front door
matters less — but it is not the number to use for capacity planning
against a real production origin (php-fpm, object caching, a CDN in
front). The single-connection latency numbers above are the honest signal
for per-request overhead; this chart is the honest signal for
"what happens when the origin can't keep up," which is a different,
also-useful question.

### Where DetCordon's time actually goes

<ChartPanel
  title="Time breakdown — one DetCordon request (p50)"
  subtitle="Derived, not separately profiled: gatekeeper's own measured overhead over baseline stands in for the pure proxy/network cost; the rest of DetCordon's overhead is attributed to CRS rule evaluation."
>
  <DonutChart
    unit=" ms"
    centerLabel={`${(BENCH.detcordonTimeBreakdownMs.wordpress + BENCH.detcordonTimeBreakdownMs.proxyOverhead + BENCH.detcordonTimeBreakdownMs.modsecurity).toFixed(0)}`}
    centerSublabel="ms total"
    segments={[
      { label: 'WordPress itself', value: BENCH.detcordonTimeBreakdownMs.wordpress, color: PALETTE.wordpress },
      { label: 'Proxy / network hop', value: BENCH.detcordonTimeBreakdownMs.proxyOverhead, color: PALETTE.gatekeeper },
      { label: 'ModSecurity + CRS (both directions)', value: BENCH.detcordonTimeBreakdownMs.modsecurity, color: PALETTE.detcordon },
    ]}
  />
</ChartPanel>

The rule engine is most of the story, even at the p50 (median) request —
and per the latency chart above, its share of the total only grows at the
tail. WordPress's own response time and the raw network hop barely move
between configurations; the bulk of DetCordon's overhead is the deliberate
choice to run the full CRS ruleset against both the request *and* the
response, which most WAF deployments skip on the response side. DetCordon
doesn't skip it, because the response is often where an actual exploit
attempt or data leak shows up.

### Resource footprint

<div style={{display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '4px 24px'}}>

<ChartPanel title="Memory (RSS)" subtitle="Sampled directly from each proxy's process">
  <DonutChart
    unit=" MB"
    segments={[
      { label: 'Gatekeeper', value: BENCH.memoryMb.gatekeeper, color: PALETTE.gatekeeper },
      { label: 'DetCordon', value: BENCH.memoryMb.detcordon, color: PALETTE.detcordon },
    ]}
  />
</ChartPanel>

<ChartPanel title="CPU under load" subtitle="Measured via /proc CPU-time deltas during a concurrent run">
  <DonutChart
    unit="%"
    segments={[
      { label: 'Gatekeeper', value: BENCH.cpuPctUnderLoad.gatekeeper, color: PALETTE.gatekeeper },
      { label: 'DetCordon', value: BENCH.cpuPctUnderLoad.detcordon, color: PALETTE.detcordon },
    ]}
  />
</ChartPanel>

<ChartPanel title="Disk footprint" subtitle="Binary (+ CRS ruleset + libmodsecurity for DetCordon)">
  <DonutChart
    unit=" MB"
    segments={[
      { label: 'Gatekeeper', value: BENCH.diskMb.gatekeeper, color: PALETTE.gatekeeper },
      { label: 'DetCordon', value: BENCH.diskMb.detcordon, color: PALETTE.detcordon },
    ]}
  />
</ChartPanel>

<ChartPanel title="Bandwidth (transfer rate)" subtitle="Kbytes/sec for the same page, concurrent run">
  <DonutChart
    unit=" KB/s"
    segments={[
      { label: 'Plain WordPress', value: BENCH.transferKbSecConcurrent.wordpress, color: PALETTE.wordpress },
      { label: 'Gatekeeper', value: BENCH.transferKbSecConcurrent.gatekeeper, color: PALETTE.gatekeeper },
      { label: 'DetCordon', value: BENCH.transferKbSecConcurrent.detcordon, color: PALETTE.detcordon },
    ]}
  />
</ChartPanel>

</div>

The CPU and latency-tail story line up: DetCordon's process spends real,
measurable CPU time evaluating rules (5x gatekeeper's), and that's exactly
where the tail-latency cost above comes from. Disk and memory footprint,
by contrast, turned out much closer between the two than intuition
suggests — modern ModSecurity + a trimmed CRS ruleset is not a heavyweight
dependency; the *runtime* cost is where the real price is paid, not the
install size.

### How this was measured

One fresh, stock WordPress install (latest core, official SQLite Database
Integration plugin, no MySQL, no custom theme or plugins beyond the
default) served by PHP 8.4's built-in single-threaded development server,
fronted by three genuinely-running, release-build binaries on the same
host and port-separated: plain (`:9000`), gatekeeper's real `rust-gateway`
release binary (`:9001`), and DetCordon's real `waf` release binary
(`:9002`) loaded with the actual, unmodified OWASP Core Rule Set (`ab`'s
own error log confirmed zero rule-load failures, and a synthetic
SQL-injection-shaped request was sent to confirm the CRS-loaded `waf`
process actually processed it through the rule engine before passing it
through, per `DetectionOnly`).

Load was generated with Apache Bench (`ab`): a single-connection run
(`-n 200 -c 1`) for the latency percentiles, and a 20-concurrent-connection
run (`-n 1000 -c 20`) for throughput and bandwidth. CPU was measured as a
true elapsed-time CPU-percentage (via `/proc/[pid]/stat` utime+stime deltas
across a fixed sampling window that overlapped a live load run, not `ps`'s
misleading since-process-start average), and memory as resident set size
(`VmRSS`) directly from `/proc/[pid]/status`. Disk figures are each
binary's actual file size, plus (for DetCordon) the linked
`libmodsecurity.so` and the CRS ruleset's own on-disk size — common
transitively-shared system libraries (OpenSSL, libcurl, and so on, which
virtually any server already has installed) are excluded from that figure
as not meaningfully attributable to either product.

**Read the caveat under the throughput chart above before treating the
concurrent-run req/sec numbers as a capacity-planning input** — a
single-threaded PHP dev server was a deliberate, honestly-disclosed choice
(it's the one origin every configuration could share identically, with
nothing WordPress-specific in the way), but it becomes the bottleneck under
concurrency, which compresses the differences between front doors at that
specific point. The single-connection latency percentiles are the cleaner
signal for per-request overhead specifically; a production deployment
behind php-fpm and a real database will have different absolute numbers for
all three configurations, but there's no reason to expect the *relative*
cost of each front door to change in kind, only in scale.

---

## DetCordon

**[DetCordon](/products/detcordon)** is a containment-first sandbox: it
assumes something will get through eventually, and gives you a safe,
observed place for that to happen instead of a silent compromise.

### Logos — the technical case

DetCordon runs the full OWASP Core Rule Set against both the request and
the response, in `DetectionOnly` mode, so nothing is ever blocked based on
a rule match that could be a false positive — every event is logged with
full context instead. Underneath that, eBPF telemetry watches syscalls
(`execve`, `connect`, `write`) at the kernel level, and Firecracker microVMs
give each detonation a hardware-isolated, disposable environment with a
hard TTL. Anything a compromised process drops — a webshell, a
dropper, a C2 payload — gets hashed, encrypted to your key, and shipped to
a separate sink host as evidence, automatically. The [performance numbers
above](#performance-metrics) are the honest cost of that depth: tens of
milliseconds of added latency at the median, growing sharply at the tail
under load, in exchange for inspecting a response body most WAFs never look
at.

### Ethos — why trust it

DetCordon's own claims are checked, not asserted: the containment
model has a public [reviewer checklist](/products/detcordon/containment-assurance)
mapping every non-negotiable rule (sandbox/sink host separation, TTL
enforcement, `DetectionOnly` never silently upgrading to blocking, sample
bodies never printed in cleartext) to the exact code and test that
enforces it, so a technical buyer doesn't have to take our word for it. The
codebase carries a security-audit trail as a first-class practice, not an
afterthought bolted on before a sales call.

### Pathos — what's actually at stake

The realistic alternative to DetCordon isn't "nothing bad happens" — it's
finding out about a compromise from a customer, a breach-notification
lawyer, or a ransomware note, weeks after the fact, with no idea what got
in or what it took. DetCordon's entire value is turning that unknown into
a known: a hashed sample, a timeline of syscalls, a captured payload you
can hand to your incident response team the same day instead of the same
quarter. For a stakeholder signing off on the budget, that's the
difference between an incident that costs an afternoon of engineering time
and one that costs a disclosure filing.

**Best fit:** teams who run — or host on behalf of others — a WordPress (or
any web) property that's an attractive target, and want a real answer to
"what happens if we're wrong about our defenses" instead of hoping they
never find out.

---

## Gatekeeper

**[Gatekeeper](/products/articulate/gatekeeper)** is the opposite bet: keep
the obvious, common compromise path closed, as cheaply as possible, on
every single request, forever.

### Logos — the technical case

Gatekeeper's entire request path for ordinary traffic is a thin, bounded
pass-through — buffer, forward, done — with a dedicated hardened endpoint
for the one thing that actually matters for WordPress compromise in
practice: file uploads. `setup_wp.sh` checks every plugin and theme against
the Wordfence vulnerability feed *before* installation, the filesystem is
read-only except `wp-content/uploads` (enforced by `harden_runtime_fs.sh`),
dangerous PHP functions are disabled at the runtime level, and `auditd`
rules watch the immutable tree for anything that shouldn't change. The
[performance numbers above](#performance-metrics) show why this is
sustainable at scale: a couple of milliseconds of added latency at the
median, no meaningful tail-latency growth under load, because it isn't
running a rule engine against every byte — it's closing a specific,
well-understood door.

### Ethos — why trust it

Gatekeeper doesn't just harden the runtime and hope — it ships its own
incident-response tooling (`incident_response.sh`, `auditd_incident_hook.sh`,
GPG-encrypted incident bundles) on the assumption that even a hardened
system deserves a real forensic path if something does go wrong. GraphQL
depth/complexity/introspection limits and client-cert-gated admin access are
built in, not optional add-ons. This is written by people who assume they
will eventually be tested, not people selling a guarantee.

### Pathos — what's actually at stake

Most WordPress compromises aren't exotic — they're an out-of-date plugin, an
uploads directory that accepts a `.php` file with a `.jpg` extension, an
admin panel reachable from anywhere on the internet. That's a boring,
preventable, entirely avoidable kind of bad day, and it's the one that ends
a lot of small businesses' relationship with their web presence overnight.
Gatekeeper's pitch to a customer or general audience is simple: the fix
for the common case shouldn't cost you anything you'd notice, and now it
doesn't.

**Best fit:** anyone running a production WordPress site who wants the
common compromise paths closed by default, with negligible performance
cost, and doesn't need (or can't afford the latency of) full
malware-capture depth on every request.

---

## Using both together

They're not mutually exclusive, and the performance numbers above are
exactly why layering them makes sense: gatekeeper in front of the public
site, cheap enough to run on every request forever; DetCordon fed a copy of
traffic — or standing in for a specific high-risk surface like an upload
endpoint or a staging environment — where the extra tens of milliseconds is
a cost you're willing to pay for the depth of what gets captured. Most
teams don't need to choose one architecture for the whole site; they need
the cheap door closed everywhere, and the expensive camera pointed at the
place they're most worried about.

**[Request a DetCordon pilot →](mailto:sales@ragbaz.cc?subject=DetCordon%20pilot%20request)**
**[Ask about gatekeeper →](mailto:sales@ragbaz.cc?subject=Gatekeeper%20inquiry)**
