---
title: PHP VM
sidebar_position: 7
description: PHP runtime exploration with WordPress-related examples.
---

# PHP VM

Path: `experiments/phpvm`

`phpvm` is a sparse runtime experiment.

## Observable scope

- a `src/` directory,
- `examples/wordpress-embed`,
- `examples/wordpress-sqlite`.

## Current interpretation

Even without strong prose docs, the naming suggests a PHP runtime or packaging experiment, with WordPress as the most important validation target.

This experiment likely informs some of the WordPress runtime thinking seen later in `gatekeeper` and `wp-sidecar`.
